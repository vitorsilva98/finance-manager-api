Casos de teste EntryController

[POST] /api/v1/finance/entries
    ERRO:
        1. Token JWT válido sem prefixo "Bearer"
        2. Token JWT inválido com prefixo "Bearer"
        3. Token JWT expirado com prefixo "Bearer"
        4. Token JWT válido com prefixo "Bearer"
            4.1.  Campo "amount" nulo
            4.2.  Campo "amount" vazio
            4.3.  Campo "amount" negativo
            4.4.  Campo "amount" zero
            4.5.  Campo "amount" com mais de duas casas decimais
            4.6.  Campo "amount" com mais de oito casas inteiras
            4.7.  Campo "categoryId" com formato diferente de UUID
            4.8.  Campo "categoryId" nulo
            4.9.  Campo "categoryId" vazio
            4.10. Campo "categoryId" em formato UUID porém sem nenhum registro equivalente cadastrado
            4.11. Campo "paymentMethod" vazio
            4.12. Campo "paymentMethod" nulo
            4.13. Campo "paymentMethod" com valores diferentes de PIX, CASH, DEBT, CREDIT, BANK_ACCOUNT_DEPOSIT
            4.14. Campo "type" vazio
            4.15. Campo "type" nulo
            4.16. Campo "type" com valores diferentes de INCOME, PURCHASE
            4.17. Campo "dateTime" com data no futuro
            4.18. Campo "dateTime" com formato diferente de dd/MM/yyyy HH:mm
            4.19. Campo "description" vazio

    SUCESSO:
        1. Token JWT válido com prefixo "Bearer"
            1.1. Campo "dateTime" = null (deve pegar a hora de agora)
            1.2. Campo "datetime" != null (deve pegar a hora do request)

[GET] /api/v1/finance/entries
    ERRO:
        1. Token JWT válido sem prefixo "Bearer"
        2. Token JWT inválido com prefixo "Bearer"
        3. Token JWT expirado com prefixo "Bearer"

    SUCESSO:
        1. Token JWT válido com prefixo "Bearer"

[GET] /api/v1/finance/entries/{id}
    ERRO:
        1. Token JWT válido sem prefixo "Bearer"
        2. Token JWT inválido com prefixo "Bearer"
        3. Token JWT expirado com prefixo "Bearer"
        4. Token JWT válido com prefixo "Bearer"
            4.1. Path variable "id" com id diferente do formato UUID
            4.2. Path variable "id" com id em formato UUID porém sem nenhum registro equivalente cadastrado

    SUCESSO:
        1. Path variable "id" com id em formato UUID com registro equivalente cadastrado e token JWT válido com prefixo "Bearer"

[PATCH] /api/v1/finance/entries/{id}
    ERRO:
        1. Token JWT válido sem prefixo "Bearer"
        2. Token JWT inválido com prefixo "Bearer"
        3. Token JWT expirado com prefixo "Bearer"
        4. Token JWT válido com prefixo "Bearer"
            4.1. Path variable "id" com id diferente do formato UUID
            4.2. Path variable "id" com id em formato UUID porém sem nenhum registro equivalente cadastrado
            4.3. Campo "reversalDateTime" com data no futuro
            4.4. Campo "reversalDateTime" com formato diferente de dd/MM/yyyy HH:mm
            4.5. Usuário não possui ROLE_ADMIN e está tentando estornar um lançamento de outro usuário

    SUCESSO:
        1. Token JWT válido com prefixo "Bearer"
            1.1. Campo "reversalDateTime" = null (deve pegar a hora de agora)
            1.2. Campo "reversalDateTime" != null (deve pegar a hora do request)
            1.3. Usuário não possui ROLE_ADMIN e está tentando estornar um lançamento próprio
            1.4. Usuário possui ROLE_ADMIN e está tentando estornar um lançamento próprio
            1.5. Usuário possui ROLE_ADMIN e está tentando estornar um lançamento de outro usuário

[DELETE] /api/v1/finance/entries{id}
    ERRO:
        1. Token JWT válido sem prefixo "Bearer"
        2. Token JWT inválido com prefixo "Bearer"
        3. Token JWT expirado com prefixo "Bearer"
        4. Token JWT válido com prefixo "Bearer"
            4.1. Path variable "id" com id diferente do formato UUID
            4.2. Path variable "id" com id em formato UUID porém sem nenhum registro equivalente cadastrado
            4.3. Path variable "id" com id em formato UUID com registro equivalente cadastrado porém usuário não possui ROLE_ADMIN

    SUCESSO:
        1. Path variable "id" com id em formato UUID com registro equivalente cadastrado,
            usuário possui ROLE_ADMIN e token JWT válido com prefixo "Bearer"
